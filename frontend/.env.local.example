# Copy to .env.local for local development
# Next.js reads this at build time AND at runtime (server-side only unless prefixed with NEXT_PUBLIC_)

# FastAPI backend URL — used by the Next.js rewrite proxy
# Change to your Railway API service URL in production
NEXT_PUBLIC_API_URL=http://localhost:8000
